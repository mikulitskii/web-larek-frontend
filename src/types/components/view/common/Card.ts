export interface CardData {
	id: string;
	title: string;
	description: string;
	image: string;
	category: string;
	price: number;
}

export interface CardSettings {
	title: string;
	description: string;
	image: string;
	category: string;
	price: string;
}
